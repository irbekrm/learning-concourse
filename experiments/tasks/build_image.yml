---
platform: linux
image_resource:
  type: registry-image
  source:
    repository: vito/oci-build-task
inputs:
- name: git_repo
outputs:
- name: image
run:
  path: build
params:
  CONTEXT:
  UNPACK_ROOTFS:
