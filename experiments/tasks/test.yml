---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
- name: learning-concourse

run:
  path: ./learning-concourse/experiments/tasks/test.sh

params:
  ROLE_ID:
  SECRET_ID:
  VAULT_URL: